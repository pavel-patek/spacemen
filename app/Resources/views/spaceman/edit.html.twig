{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('spaceman_overview') }}" class="btn">Back</a>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}